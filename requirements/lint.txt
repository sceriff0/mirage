# The Python linter CI runs. BLOCKING: `ruff` is in the `all-tests` gate's `needs:`
# alongside the test jobs, so a ruff failure fails the build.
#
# Pinned so a new ruff release cannot turn a green build red without a commit. Its
# configuration is ruff.toml; keep the pin and that file in step.
ruff==0.16.6
