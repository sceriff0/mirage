# EXPORT_SPATIALDATA / JOIN_FLOWPATH — scverse serialization stack, CPU only.
#
# LOCKED, not bounded (ruling R6). Every line here was a lower bound until 2026-09-02, so
# two builds a month apart produced two different images with nothing recording which one
# generated a published .zarr. The argument the old header made for bounds is real — the
# SpatialData on-disk format is still moving, and 0.8.0 switched to zarr 3 and writes NGFF
# "0.5-dev-spatialdata" — and it is exactly why these are a DATED, RE-RESOLVABLE lock
# rather than a guess. Bumping is one command plus a rebuild:
#
#     docker build --platform linux/amd64 -f containers/spatialdata/Dockerfile -t mirage-spatialdata:lock .
#     docker run --rm mirage-spatialdata:lock python -m pip freeze | grep -iE \
#       '^(spatialdata|zarr|ome-zarr|anndata|geopandas|shapely|pyarrow|dask|tifffile|imagecodecs|scipy|pandas|numpy)=='
#
# Resolved 2026-09-03 on CPython 3.12.14 from containers/spatialdata/Dockerfile's own build,
# built --platform linux/amd64 (the image's shipped platform; this host is arm64, and an
# arm64-resolved lock can differ in wheels, `nvidia-*`, `triton`, so an arm64 resolution
# would be wrong for the published image).
# Only the thirteen packages this file names are pinned; the transitive closure is left to
# pip, because pinning it would make every upstream patch release a merge conflict in a
# file nobody can review.
#
# DELIBERATELY NOT CONSTRAINED. There is no `-c constraints.txt` line here and there must
# not be one: this is the documented python:3.12 / zarr>=3 island. The harmonised set pins
# zarr==2.18.3 for tifffile's aszarr region reads, which spatialdata>=0.8.0 cannot use.
# tests/test_ci_stack_pinned.py lists this image as the standing exception, and
# test_container_harmonisation.py's PY312_ISLAND branch is what lets these lines be `==`
# without being compared against the harmonised table.
#
# PYTHON 3.12 IS EXERCISED BY NO CI LEG, and adding one would not close the gap.
# _test-suite.yml's python-tests matrix is 3.10/3.11 and installs requirements/ci.txt,
# which is `-c constraints.txt` plus segeval.txt and tiled.txt — zarr 2.18.3 and
# anndata<0.12, i.e. NOT what this image ships. A 3.12 leg would test a combination no
# image runs while still never importing spatialdata. The real closure is a job that runs
# tests/test_join_flowpath.py inside bolt3x/mirage-spatialdata; requirements/ci.txt's
# anndata note says the same thing about the same gap. Until then, this image's own
# containers/spatialdata/smoke.sh — run at build time AND by containers.yml against the
# built image — is the only thing that exercises the stack, and it is why that script
# imports spatialdata, anndata, geopandas, shapely, zarr, dask and tifffile by name.
spatialdata==0.8.0
zarr==3.3.0            # NOT the 2.18 the tiled image pins; spatialdata >= 0.8.0 needs 3.x
ome-zarr==0.19.1
anndata==0.13.3.post0

# Shapes are GeoParquet-backed GeoDataFrames; GEOS comes from libgeos-c1v5 in the image.
geopandas==1.1.4
shapely==2.1.2
pyarrow==25.0.1         # GeoParquet writer for shapes/, and Parquet for any points/

# Lazy, chunked reads of the merged pyramid — peak memory is one chunk, not one slide.
# THE `[array]` EXTRA IS LOAD-BEARING and `pip freeze` drops it: bin/export_spatialdata.py
# reads through dask.array, which the bare distribution does not pull in.
dask[array]==2026.8.0
tifffile==2026.8.23
imagecodecs==2026.8.16     # zstd/LZW codecs used by MERGE_AND_PYRAMID's OME-TIFF

# cKDTree for the QC-residual -> cell_mask spatial join.
scipy==1.18.1
pandas==3.0.5
numpy==2.5.2
