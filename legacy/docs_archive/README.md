# Archived Documentation

This folder contains pre-refactor markdown documentation that is no longer part of the active ReadTheDocs/MkDocs site.

The canonical published docs are now the markdown files under `docs/` referenced by `mkdocs.yml`.
