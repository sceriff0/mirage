# The minimum number of tests the Python suite must COLLECT. A RATCHET: it may
# only go up. Read by tests/conftest.py, enforced only when MIRAGE_STRICT_SKIPS
# is set (CI's blocking full-suite step).
#
# Measured with the suite's standard exclusions, after generating the fixtures:
#
#   python tests/testdata/generate_complete_testdata.py
#   pytest tests/ --ignore=tests/testdata --ignore=tests/modules \
#                 --ignore=tests/subworkflows --ignore=tests/integration
#
# and equal to passed + skipped. Measured 2026-09-01 on ci/merge-staging, the
# integration of all six CI-redesign branches (handoff guards + Phases 1-7):
# 1280 passed, 12 skipped, 0 failed.
#
# RE-MEASURED 2026-09-01 on ci/merge-staging after containers/regqc's dead
# basicpy/dipy dependencies were deleted: 1280 passed, 12 skipped, unchanged.
# That fix adds and removes no test -- it deletes two pip lines -- so the number
# below is confirmed current rather than carried over unchecked. (An earlier
# attempt at that fix exempted the image from requirements/constraints.txt
# instead, which DID move the count to 1294 by adding two derived
# parametrisations; deleting the dead dependencies is the shipped fix and the
# count returns here.)
#
# The previous entry was 1262, measured on ci/phase-6-7 alone. The integrated
# tree is larger than any one branch because the six are disjoint additions:
# the handoff guards (test_figures_match_the_pipeline.py,
# test_join_reg_residuals_convention.py) never met Phases 1-7, and each phase
# added its own guards on top of its predecessor's first commit.
#
# 37 node ids present on some branch are absent here, every one accounted for:
# 13 tests Phase 1 deleted with the per-package pin model (eec3ff0), 14 that
# Phase 5 renamed (test_the_release_gate_runs_every_check_ci_blocks_on ->
# test_the_shared_test_suite_runs_every_blocking_check), 6 that are the same
# parametrised cases under shifted expectedN indices, 2 that followed
# build-images.yml into containers.yml, 1 renamed with its widened scope
# (test_the_workflows_ -> test_the_scanned_files_actually_install_something),
# and 1 renamed per-JOB (test_every_pytest_workflow_ -> _job_installs_torch_and_kornia).
#
# The check is `>=`, so adding tests never fails it. But a floor that is allowed
# to fall arbitrarily far behind stops being a floor, so conftest.py also fails
# once the suite has grown FLOOR_DRIFT (50) tests past this number, with the
# replacement value in the message. Re-ratcheting is then one line, here.
#
# RE-MEASURED 2026-09-02 on the 1.0.0 release branch, plan 01 (suite truth):
# tests/test_fixtures_have_a_producer.py gained 2 cases and
# tests/test_export_geojson_fixture_contract.py added 4. That round landed the
# floor at 1298, but never re-ran `pytest --collect-only` afterward: Task 6's
# BLOCKING_CHECKS gained a row (a parametrised case of
# test_the_shared_test_suite_runs_every_blocking_check), +1, which 1298 never
# counted, and the review round below adds ADVISORY_SUITES' `docs strict site
# build` entry (I2), another parametrised case, +1. 1298 + 1 + 1 = 1300.
#
# RE-MEASURED AGAIN 2026-09-02, final-review fix wave (I1/I2/M1/M2/M4/M5):
# `pytest --collect-only -q tests/ --ignore=tests/testdata --ignore=tests/modules
# --ignore=tests/subworkflows --ignore=tests/integration` reports "1300 tests
# collected", and a full run with MIRAGE_STRICT_SKIPS=1 gives 1288 passed, 12
# skipped, 0 failed -- 1288 + 12 = 1300, matching the collect count exactly.
#
# RE-MEASURED 2026-09-02 on release/p02-cleanliness (phase 02, tasks 3 + 3b),
# rebased onto plan 01's final tip (35b30db = 1300): task 3 added one
# BLOCKING_CHECKS case (the "ruff format" needle), +1, and task 3b added
# tests/test_nf_test_shards_cover_the_suite.py's two cases, +2.
# 1300 + 1 + 2 = 1303, confirmed by `pytest --collect-only`.
# RE-MEASURED 2026-09-02 on release/p02-cleanliness (release 1.0, phase 02,
# tasks 3 + 3b): 1290 passed, 12 skipped, 0 failed -- 1303 collected.
# Two of this phase's own commits account for 3 of the 4 tests above the
# previous floor: task 3 (`:white_check_mark: Make ruff format --check
# blocking in the shared suite`) added one BLOCKING_CHECKS case (the "ruff
# format" needle), and task 3b (`:zap: Shard the nf-test stub job four ways`)
# added tests/test_nf_test_shards_cover_the_suite.py's two cases. The
# remaining 1 predates this phase: it is drift from plan 01 commits already on
# this branch between the 1298 measurement (cf001d3) and phase 02's start
# (8e74c9e) -- e.g. 1c25a99's vacuous-pass fix -- that had grown the suite
# within FLOOR_DRIFT (50) without yet being re-ratcheted. Re-ratcheting here
# folds that drift in rather than leaving it unaccounted.
#
# RE-MEASURED 2026-09-02 on release/p02-cleanliness, task 7 (bin/ entry-point
# idiom guard): 1321 passed, 13 skipped, 0 failed -- 1335 collected, up from
# the prior entry's 1303. The increase is exactly
# tests/test_bin_entrypoint_idiom.py's 32 new cases (31 parametrised
# entry-point checks + 1 tripwire); this task's own baseline run (before
# adding that file) was already 1289 passed / 13 skipped -- 1 skip more than
# the prior entry's 1290/12 split at the same 1303 total. That extra skip is
# not environment drift: Task 5 (:fire: Delete bin/registration_benchmark.py
# from this branch) added
# tests/test_no_dead_bin_modules.py::test_bin_utils_module_has_a_real_importer[reg_benchmark.py]
# to tests/expected_skips.txt, because bin/utils/reg_benchmark.py lost its
# only production importer when bin/registration_benchmark.py was deleted for
# release 1.0. That commit landed on this branch between the 1303 measurement
# and this task's baseline, which is why the split moved -- not something
# this task introduced.
#
# RE-MEASURED 2026-09-02 on release/p02-cleanliness, task 9 (docstring
# coverage guard): 1323 passed, 13 skipped, 0 failed -- 1337 collected, up
# from the prior entry's 1335. The increase is exactly
# tests/test_docstring_coverage.py's 2 new cases
# (test_the_walk_actually_visited_the_tree and
# test_no_public_definition_lacks_a_docstring); no other test count moved.
#
# RE-MEASURED 2026-09-02, final-review fix wave for phase 02 (M8): `pytest
# --collect-only -q tests/ --ignore=tests/testdata --ignore=tests/modules
# --ignore=tests/subworkflows --ignore=tests/integration` reports "1338 tests
# collected", up from the prior entry's 1337. The increase is exactly
# tests/test_nf_test_shards_cover_the_suite.py's one new case,
# test_shard_strategy_is_round_robin, which asserts the nf-test-stub job's
# run script actually passes `--shard-strategy round-robin` (verified
# red/green: deleting the flag from .github/workflows/_test-suite.yml fails
# this test, restoring it passes). No other test count moved.
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, phase 10 task 4
# (generate_resource_report.py: parse requested memory/cpus and failed
# GB.h in parse_trace/rollup_by_process): 1326 passed, 13 skipped, 0
# failed -- 1339 collected, up from the prior entry's 1336. The increase
# is exactly the 3 new cases in tests/test_generate_resource_report.py
# (test_parse_trace_reads_requested_memory_and_cpus,
# test_rollup_carries_the_max_request_and_the_cost_of_failures,
# test_failed_gb_h_falls_back_to_observed_rss_when_no_request_is_recorded);
# no other test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, phase 10 task 5
# (generate_resource_report.py: Panel 1, walltime_bars_svg + short_process):
# 1331 passed, 13 skipped, 0 failed -- 1344 collected, up from the prior
# entry's 1339. The increase is exactly the 5 new cases in
# tests/test_generate_resource_report.py
# (test_short_process_keeps_only_the_leaf_name,
# test_walltime_bars_draws_one_bar_per_process_longest_first,
# test_walltime_bars_truncates_to_top_n,
# test_walltime_bars_is_empty_for_no_rows,
# test_walltime_bars_escapes_a_hostile_process_name); no other test count
# moved.

#
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, phase 10 task 6
# (generate_resource_report.py: Panel 2, memory_headroom_svg): 1335 passed,
# 13 skipped, 0 failed -- 1348 collected, up from the prior entry's 1344.
# The increase is exactly the 4 new cases in
# tests/test_generate_resource_report.py
# (test_memory_headroom_draws_a_request_track_and_an_observed_overlay,
# test_memory_headroom_skips_a_process_with_no_recorded_request,
# test_memory_headroom_marks_a_process_that_exceeded_its_request,
# test_memory_headroom_is_empty_when_nothing_is_plottable); no other test
# count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, phase 10 task 7
# (generate_resource_report.py: Panel 3, failure_cost_svg): 1338 passed,
# 13 skipped, 0 failed -- 1351 collected, up from the prior entry's 1348.
# The increase is exactly the 3 new cases in
# tests/test_generate_resource_report.py
# (test_failure_cost_draws_one_bar_per_failing_process,
# test_failure_cost_reports_the_reserved_memory_hours_not_just_the_count,
# test_failure_cost_is_empty_when_nothing_failed); no other test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, phase 10 task 8
# (generate_resource_report.py: Panel 4, size_vs_runtime_svg): 1342 passed,
# 13 skipped, 0 failed -- 1355 collected, up from the prior entry's 1351.
# The increase is exactly the 4 new cases in
# tests/test_generate_resource_report.py
# (test_size_vs_runtime_plots_one_point_per_joinable_task,
# test_size_vs_runtime_drops_a_zero_byte_input_from_the_log_axis,
# test_size_vs_runtime_ignores_unjoined_rows,
# test_size_vs_runtime_escapes_a_hostile_process_name); no other test count
# moved.

#
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, phase 10 fix round 1
# (generate_resource_report.py: +-inf/nan crash fix across all four Panel
# 5-8 functions plus fmt_secs/fmt_bytes): 1365 passed, 13 skipped, 0 failed
# -- 1378 collected, up from the prior entry's 1355. The increase is exactly
# the 23 new cases in tests/test_generate_resource_report.py
# (test_fmt_secs_returns_a_sentinel_for_non_finite_input,
# test_fmt_bytes_returns_a_sentinel_for_non_finite_input, and seven
# parametrized [inf, -inf, nan] cases -- test_walltime_bars_drops_a_non_finite_realtime_total,
# test_memory_headroom_drops_a_non_finite_request,
# test_memory_headroom_drops_a_non_finite_observed_peak,
# test_failure_cost_drops_a_non_finite_failed_gb_h,
# test_failure_cost_drops_a_non_finite_failed_realtime,
# test_size_vs_runtime_drops_and_counts_a_non_finite_input_bytes,
# test_size_vs_runtime_drops_and_counts_a_non_finite_realtime); no other
# test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, phase 10 task 9
# (generate_resource_report.py: build_html renders the four panels instead of
# six tables; Run Totals is the one surviving table and gains a peak %CPU
# row; _html_table deleted): 1368 passed, 13 skipped, 0 failed -- 1381
# collected, up from the prior entry's 1378. The increase is exactly the 3
# new cases in tests/test_generate_resource_report.py
# (test_the_report_is_plots_not_tables,
# test_run_totals_survives_with_every_number_it_had,
# test_a_panel_with_no_data_degrades_to_a_sentence_not_a_crash); four other
# tests were rewritten in place (same names replaced 1:1, net zero) and one
# (test_build_html_exit_dash_excluded_from_failures) was left unedited; no
# other test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, phase 10 tasks 10-12
# (bin/generate_resource_report.py's stdlib-only constraint is now guarded, and
# docs/CHANGELOG were updated for the four-panel report; no bin/ or lib/ code
# changed): 1370 passed, 13 skipped, 0 failed -- 1383 collected, up from the
# prior entry's 1381. The increase is exactly the 2 new cases in
# tests/test_resource_report_is_stdlib_only.py
# (test_the_resource_report_imports_only_the_standard_library,
# test_the_guard_would_notice_a_third_party_import). That file's docstring
# originally named `main.nf` as the caller it documents, which tripped
# test_no_guard_parses_nextflow_source_privately's `.nf`-substring discovery
# heuristic even though the guard reads no Nextflow source at all. Exempted via
# a new PROSE_ONLY_NF_MENTIONS list reusing WORKFLOW_YAML_GUARDS's
# already-established construct/glob-path check; no other test count moved.
#
# heuristic even though the guard reads no Nextflow source at all; fix round 1
# tried a new PROSE_ONLY_NF_MENTIONS exemption category for this, but review
# constructed a real private Nextflow parser (root named HERE, not
# ROOT/REPO/REPO_ROOT) the category would have wrongly certified as
# prose-only, so that category was reverted and the three prose mentions
# reworded instead to contain no `.nf`/`.groovy`/`modules.config` substring at
# all -- test_nfmodel.py itself is therefore byte-identical to the prior
# entry's tree and contributes no new case; no other test count moved.
#
# RE-MEASURED 2026-09-02 on release/p10-resource-report, final-review fix wave
# (I1/M2/M3/M4/M5/M6/M7 against bin/generate_resource_report.py, main.nf and
# tests/main.nf.test): `pytest --collect-only -q tests/ --ignore=tests/testdata
# --ignore=tests/modules --ignore=tests/subworkflows --ignore=tests/integration`
# reports "1394 tests collected", up from the prior entry's 1385. The increase
# is exactly the 9 new cases in tests/test_generate_resource_report.py
# (test_size_vs_runtime_legend_ranked_by_point_count_not_first_appearance,
# test_size_vs_runtime_caps_points_per_series,
# test_size_vs_runtime_reports_drop_count_when_nothing_is_plottable,
# test_build_html_size_panel_is_not_falsely_empty_when_everything_dropped,
# test_parse_percent_rejects_non_finite_strings,
# test_run_totals_peak_cpu_guards_against_non_finite,
# test_walltime_bars_percentage_is_share_of_total_not_longest,
# test_cli_exits_nonzero_on_a_header_only_trace,
# test_no_trace_message_does_not_show_the_broken_cli_boolean_form); no other
# test count moved. A full run with MIRAGE_STRICT_SKIPS=1 gives 1381 passed,
# 13 skipped, 0 failed -- 1381 + 13 = 1394, matching the collect count exactly.
# One of the new docstrings (test_cli_exits_nonzero_on_a_header_only_trace)
# first named the entry script by its literal filename, which -- combined with
# this file's pre-existing `.read_text()` calls -- tripped
# test_nfmodel.py's `_discover_nf_source_readers()` substring heuristic even
# though the test parses no Nextflow source at all; reworded to name it only
# as "the entry script", the same fix shape as the prior entry's three prose
# mentions, and test_nfmodel.py itself is unchanged.
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 1 (QC report section-list
# characterisation test): 1324 passed, 13 skipped, 0 failed -- 1337 collected, up
# from the prior entry's 1336. The increase is exactly one test,
# test_report_section_headings_are_exactly_these, added to
# tests/test_generate_qc_report.py; no other test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 2 (cut versions_section
# and parse_versions_yml): 1321 passed, 13 skipped, 0 failed -- 1334 collected,
# DOWN from the prior entry's 1337. The Software Versions section's rendering is
# deliberately deleted, and with it its three tests
# (test_parse_versions_yml_two_level, test_versions_section_renders_table,
# test_versions_section_missing_file_is_graceful); no other test count moved.
# Lowering the floor here is the documented escape hatch for a deliberate
# removal, not drift.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 3 (cut manifest_section):
# 1319 passed, 13 skipped, 0 failed -- 1332 collected, DOWN from the prior
# entry's 1334. The Sample Manifest section's rendering is deliberately
# deleted, and with it its two tests (test_manifest_section,
# test_manifest_section_escapes_html_special_patient_id); no other test count
# moved. Same documented escape hatch as task 2.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 4 (the pure-math
# plotting seams: _finite / _percentile / _bin_counts): 1324 passed, 13
# skipped, 0 failed -- 1337 collected, up from the prior entry's 1332. The
# increase is exactly the five new cases in tests/test_generate_qc_report.py
# (test_finite_drops_none_nan_bool_and_strings,
# test_percentile_is_nearest_rank_and_none_on_empty,
# test_bin_counts_puts_the_maximum_in_the_last_bin,
# test_bin_counts_widens_a_degenerate_range_so_the_single_bar_is_drawable,
# test_bin_counts_on_nothing_finite_is_empty_not_an_exception); no other test
# count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 5 (_histogram_svg, the
# bar-chart rendering primitive): 1330 passed, 13 skipped, 0 failed -- 1343
# collected, up from the prior entry's 1337. The increase is exactly the six
# new cases in tests/test_generate_qc_report.py
# (test_histogram_svg_draws_one_rect_per_non_empty_bin,
# test_histogram_svg_labels_the_axes_and_states_n,
# test_histogram_svg_draws_one_dashed_rule_per_marker,
# test_histogram_svg_ignores_an_unplottable_rule_rather_than_crashing,
# test_histogram_svg_on_empty_input_is_a_notice_not_an_svg,
# test_histogram_svg_escapes_its_title_and_axis_label); no other test count
# moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 6
# (_error_distribution_svg, the per-stage error view): 1334 passed, 13
# skipped, 0 failed -- 1347 collected, up from the prior entry's 1343. The
# increase is exactly the four new cases in tests/test_generate_qc_report.py
# (test_error_distribution_svg_marks_p50_and_p90,
# test_error_distribution_svg_states_n_so_a_one_slide_run_is_honest,
# test_error_distribution_svg_on_empty_input_is_a_notice,
# test_error_distribution_svg_drops_missing_values_before_binning); no other
# test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, fix round 1 for tasks 4-6
# (`_finite` dropped NaN via `v != v` but not `+-inf`, so an Infinity metric
# crashed `_bin_counts` with `ValueError: cannot convert float NaN to
# integer`): 1336 passed, 13 skipped, 0 failed -- 1349 collected, up from the
# prior entry's 1347. The increase is exactly two new cases in
# tests/test_generate_qc_report.py (test_finite_drops_infinities,
# test_bin_counts_drops_infinities_rather_than_crashing); no other test count
# moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 7 (replace the STARE
# per-slide TRE table with per-stage plots -- _tiled_tre_tables ->
# _tiled_tre_plots): 1337 passed, 13 skipped, 0 failed -- 1350 collected, up
# from the prior entry's 1349. tests/test_generate_qc_report.py lost two
# table-shaped tests (test_tiled_tre_table_renders_headline_numbers,
# test_fanout_tre_without_post_refinement_shows_na) and gained three
# plot-shaped ones (test_tiled_tre_plots_render_headline_numbers_and_two_stage_plots,
# test_tiled_tre_plots_omit_the_refined_stage_when_it_was_not_measured,
# test_tiled_tre_plots_exclude_rejected_tiles_from_the_distribution), net +1;
# tests/test_tre_report_accepted.py swapped its two table-caption tests for
# two plot-caption tests one-for-one, net 0. No other test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 8 (replace the warp-seg
# QC metric table with per-stage plots -- _seg_qc_table/parse_seg_qc_json/_flatten
# and the never-wired seg_qc_section deleted, _seg_qc_stage_plots added): 1339
# passed, 13 skipped, 0 failed -- 1352 collected, up from the prior entry's 1350.
# tests/test_generate_qc_report.py lost two table-shaped tests
# (test_parse_seg_qc_json_flattens, test_seg_qc_section_missing) and gained four
# plot-shaped ones (test_seg_qc_stage_plots_render_one_plot_per_stage,
# test_seg_qc_stage_plots_order_stages_canonically_not_by_file_order,
# test_seg_qc_stage_plots_fall_back_to_px_when_no_pixel_size,
# test_seg_qc_stage_plots_on_an_empty_directory_is_a_notice), net +2. No other
# test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 9 (reconciliation
# becomes a log-log scatter with the divergence band drawn): 1346 passed, 13
# skipped, 0 failed -- 1359 collected, up from the prior entry's 1352. Seven new
# cases in tests/test_generate_qc_report.py
# (test_reconciliation_scatter_draws_one_circle_per_plottable_point,
# test_reconciliation_scatter_draws_two_band_edges_and_the_identity,
# test_reconciliation_scatter_colours_a_divergent_point_differently,
# test_reconciliation_scatter_counts_out_points_a_log_axis_cannot_place,
# test_reconciliation_scatter_labels_both_axes,
# test_reconciliation_scatter_on_no_plottable_points_is_a_notice,
# test_reconciliation_section_renders_a_scatter_not_a_table); the two retained
# reconciliation tests are unchanged. No other test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 10 (per-cell residuals
# become a histogram -- parse_csv_table_head/SEG_RESIDUALS_MAX_ROWS/itertools
# deleted, _read_residual_column added): 1346 passed, 13 skipped, 0 failed --
# 1359 collected, UNCHANGED from the prior entry. tests/test_no_full_mask_unique.py
# lost its four parse_csv_table_head tests
# (test_head_parse_matches_a_full_parse_on_headers_and_rows,
# test_head_parse_counts_every_row_without_building_them,
# test_head_parse_is_not_off_by_one_at_the_boundary,
# test_head_parse_handles_a_header_only_file) and
# tests/test_generate_qc_report.py gained four histogram-shaped ones
# (test_read_residual_column_streams_values_stage_and_row_count,
# test_read_residual_column_skips_an_unparseable_value_but_counts_the_row,
# test_seg_residuals_section_is_a_histogram_not_a_row_dump,
# test_seg_residuals_section_with_no_csvs_is_a_notice), net 0. No other test
# count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, fix round 1 for tasks 7-10
# (review findings: _seg_qc_stage_plots mislabelled/mixed px and um in one
# histogram, dropped the matched-cell count, and SEG_QC_STAGE_ORDER omitted the
# tiled backend's `refined` stage; seg_residuals_section narrowed its except to
# miss a non-UTF-8 CSV's UnicodeDecodeError; _read_residual_column counted inf
# as a plotted value while _finite silently dropped it, desyncing the
# "(N unparseable)" caption from the plot's own n): 1354 passed, 13 skipped, 0
# failed -- 1367 collected, up from the prior entry's 1359. Eight new cases in
# tests/test_generate_qc_report.py
# (test_seg_qc_stage_plots_never_mix_units_and_label_each_correctly,
# test_seg_qc_stage_plots_caption_reports_matched_cell_counts,
# test_seg_qc_stage_plots_caption_omits_matched_cells_when_matching_block_absent,
# test_seg_qc_stage_order_includes_the_tiled_refined_stage,
# test_seg_qc_stage_plots_order_the_tiled_refined_stage_canonically,
# test_seg_residuals_section_reports_a_non_csv_error_read_failure_instead_of_raising,
# test_read_residual_column_counts_non_finite_values_as_unparseable,
# test_seg_residuals_section_unparseable_count_matches_the_plotted_n); no
# deletions, no other test count moved.

#
#
# RE-MEASURED 2026-09-02 on release/p09-qc-report, task 11 (stdlib-only guard
# for generate_qc_report.py): 1355 passed, 13 skipped, 0 failed -- 1368
# collected, up from the prior entry's 1367. One new case in
# tests/test_generate_qc_report.py
# (test_generate_qc_report_imports_only_the_standard_library); no deletions, no
# other test count moved.

#
#
# RE-MEASURED 2026-09-02 on phase/09-qc-report (spec Phase 4, the QC-report plot
# rework). 11 tests were DELETED with the code they covered -- 3 for
# parse_versions_yml/versions_section, 2 for manifest_section, 2 for
# parse_seg_qc_json/seg_qc_section, and 4 in test_no_full_mask_unique.py for
# parse_csv_table_head -- and replaced by unit tests for the six new plotting
# functions plus the section-list characterisation test and the stdlib-only guard.
# The net is an increase; the number below is measured, not derived.

#

# RE-MEASURED 2026-09-03: +2 for the two final-minors robustness tests (span-overflow _bin_counts, null tre_rigid _tiled_tre_plots); no other count moved.
#
# RE-MEASURED 2026-09-02 on release/p04-deepening-a, task 8 (the size-log
# header gets one owner across both languages): 1328 passed, 13 skipped,
# 0 failed -- 1341 collected, up from the prior entry's 1336. The increase is
# exactly 3: tests/test_size_log_schema_has_one_owner.py's 2 new cases
# (test_the_python_reader_expects_the_groovy_columns and
# test_the_aggregate_header_is_rendered_from_the_constant) plus
# tests/test_generate_resource_report.py's new
# test_parse_size_log_rejects_a_foreign_header. tests/test_versions_envelope.py
# was also edited in this task (ALLOWED_HANDWRITTEN deleted, and
# test_every_module_records_its_container widened to flag a module with zero
# ProcessEnvelope calls) but adds no new test function, so it contributes 0 to
# the count. Task 13 (same commit batch, deleting the two pass-through
# extract_dapi_channel wrappers) re-measured at the same 1328/13/1341 -- it
# only edits existing test bodies, no new or removed test function.

#
#
# RE-MEASURED 2026-09-02 after release plan 04 (deepening A). The phase adds five
# tests and removes none: two guards in test_versions_envelope.py (container line,
# size-log envelope), two in the new test_size_log_schema_has_one_owner.py, and one
# foreign-header case in test_generate_resource_report.py. Measured after
# generating the fixtures, with the suite's standard exclusions: `pytest
# --collect-only -q` reports "1343 tests collected", and a full run gives 1330
# passed, 13 skipped, 0 failed -- 1330 + 13 = 1343, matching the number already
# on the last line below. The prior (task 8) entry's prose undercounted -- it
# attributed only 3 of the phase's five new tests to the floor, missing
# test_every_module_records_its_container and
# test_every_size_log_module_uses_the_envelope as a distinct earlier addition in
# this phase (174eea2) -- but the number it set was already arithmetically
# correct (1338 + 2 + 3 = 1343). Left unchanged; this entry makes the accounting
# whole.

#
#
# RE-MEASURED 2026-09-03, plan 04's final-review fix wave (six findings: I1, M1,
# M2, M3, M6, M10). Two new Python test functions:
# test_segment_cellsam_delegates_to_segment_io (M2, tests/test_segment_lazy_dapi_read.py)
# and test_size_log_and_stub_write_the_same_outfile (M10,
# tests/test_versions_envelope.py). I1 and M1 only add assertions inside
# tests/lib_probe.nf (a Nextflow script, not pytest-collected); M3 and M6 touch no
# test file. `pytest --collect-only -q` reports "1345 tests collected", and a full
# run gives 1332 passed, 13 skipped, 0 failed -- 1332 + 13 = 1345.
#
# 2026-09-03, plan 05 (deepening B) after the rebase onto dev: ome_io's own suite
# (test_ome_io.py), the repointed seam tests, the only-writer guard and the
# container-harmonisation premise cases; no deletions, no skips.
#
# RE-MEASURED 2026-09-03, plan 05 Tasks 16-20 plus their fix rounds (measured
# against the 1565 set two entries above, at 3e9dd73 -- NOT the 1292 an earlier
# draft of Task 21 assumed; that baseline was already stale by the time this task
# ran, having been superseded by the ratchet directly above). `git diff
# --diff-filter=A --name-only 3e9dd73 HEAD -- tests/` names three new pytest files
# collected here: tests/test_checkpoint_writer_is_the_only_writer.py (Task 17),
# tests/test_reg_backends.py (Task 20, widened further in its own fix round without
# adding test functions), and tests/test_segment_io_expand_labels.py -- the last of
# these was newly added in this range too, despite appearing in Task 21's own
# sample provenance text as though already counted in the 1565 baseline; that text
# was written against the pre-ratchet 1292 baseline and is followed here for its
# CONTENT (which files changed) rather than its assumed STARTING NUMBER. Also
# touched, no net new/removed test functions expected: tests/test_layout.py,
# tests/test_add_cycle_writes_what_it_requires.py, tests/test_ashlar_backend_removed.py
# (one test renamed, not added), tests/test_segment_lazy_dapi_read.py, and
# tests/lib_probe.nf (a Nextflow script, not pytest-collected). No deletions, no
# skips added. Measured after generating the fixtures, with the suite's standard
# exclusions and MIRAGE_STRICT_SKIPS=1: `pytest --collect-only -q` reports "1584
# tests collected", and a full run gives 1571 passed, 13 skipped, 0 failed --
# 1571 + 13 = 1584. The prior 1565 was within FLOOR_DRIFT (50) of this number, so
# the suite did not fail on the floor; re-ratcheted anyway per instruction, to keep
# the floor a tight measurement rather than a stale one riding inside the drift band.
#
# RE-MEASURED 2026-09-02 after the DAPI before/after overlay (release plan 08):
# tests/test_registration_qc_before_after.py (17 cases) and
# tests/test_registration_qc_wiring.py (4 cases) are new; nothing was removed.
# Counted directly rather than trusting that estimate:
# test_registration_qc_before_after.py in fact carries 21 `def test_` functions,
# not 17, and a third file touched in the same range,
# tests/test_container_harmonisation.py, gained one more case
# (test_qc_reader_dispatch_stays_tifffile_only, from Task 8's ome_io
# reader-dispatch guard update) that was not called out at all. 21 + 4 + 1 = 26.
# Measured after generating the fixtures, with the suite's standard exclusions:
# `pytest --collect-only -q` reports "1610 tests collected", and a full run
# (also re-checked with MIRAGE_STRICT_SKIPS=1) gives 1597 passed, 13 skipped,
# 0 failed -- 1597 + 13 = 1610, matching exactly. 1584 + 26 = 1610.
#
# RE-MEASURED 2026-09-03, plan 08's CI-fix round. Two new cases in
# tests/test_registration_qc_before_after.py --
# test_render_before_after_warns_when_the_registered_plane_is_off_canvas and
# test_render_before_after_is_silent_when_only_the_native_differs -- covering the
# logger.warning render_before_after now emits when the REGISTERED plane is not on
# the reference canvas. The round's other edits (the tifffile RESUNIT lookup, the
# PhysicalSizeXUnit assertions, the quote-aware comment stripper in
# tests/test_container_harmonisation.py) add assertions inside existing test
# functions and collect nothing new. `pytest --collect-only -q` reports "1612 tests
# collected", and a full run with MIRAGE_STRICT_SKIPS=1 gives 1599 passed, 13
# skipped, 0 failed -- 1599 + 13 = 1612. 1610 + 2 = 1612.
#
# 2026-09-03, plan 06 (containers) Tasks 1-4 + 8a: dockerignore/digest/procps/naming
# guards and the positive REQUIRED_RUNTIME_IMPORTS table's per-entry reachability
# cases; the 16-entry unreachable-import allowlist and its two tests are gone.

#
#
# 2026-09-03, plan 06 Tasks 5-8: the bioformats jar-bake and jvm-cache route guards, and
# the reverse-rule guard (86 per-container/per-package cases: an image may not install
# what nothing reaches). No deletions, no skips.

#
#
# RE-MEASURED 2026-09-03, plan 06 Task 13 (build containers/convert and containers/regqc
# for real, re-ratchet the floor). `git diff --diff-filter=A --name-only 12f3d5b HEAD --
# tests/` names exactly two new pytest files since the 1736 measurement:
# tests/test_container_readme_matches_the_dockerfiles.py (14 cases) and
# tests/test_version_tools_are_importable_in_their_image.py (12 cases, most of it the
# `test_every_reported_tool_is_installed_in_that_image` parametrisation over every
# container that claims a tool in its README). 14 + 12 = 26, and 1736 + 26 = 1762 exactly
# -- no other test count moved. The two other files touched in that range
# (test_container_harmonisation.py's BASE_IMAGE_PROVIDES gaining a "stardist" entry,
# test_seg_backends.py's EXPECTED losing the phantom "deepcell" tool) are both data edits
# read by ALREADY-parametrised tests (parametrised over `_container_dirs()`, not over the
# dicts' own keys), so neither adds a collected case. Measured after generating the
# fixtures, with the suite's standard exclusions: `pytest --collect-only -q` reports
# "1762 tests collected", and a full run gives 1749 passed, 13 skipped, 0 failed --
# 1749 + 13 = 1762. This is also the first measurement to follow a REAL local build of
# both containers/convert and containers/regqc (Task 13's whole point) rather than a
# static read of their Dockerfiles; the build itself added no test, but did catch and
# fix two false-positive traps in containers/convert/smoke.sh's negative leg (see
# :bug: Fix two false-positive traps in convert's smoke.sh negative leg) that no static
# guard here could have found.
#
# RE-MEASURED 2026-09-02/03, release 1.0.0 plan 11 Task 2 (docs/figures retired-name
# guard). Five new cases: tests/test_figures_have_no_retired_names.py's four
# (test_no_figure_names_a_retired_backend_or_frontend,
# test_the_patterns_still_match_something_somewhere, test_the_allowlist_only_shrinks,
# test_the_allowlist_is_not_a_blanket) plus
# tests/test_figures_match_the_pipeline.py::test_a_param_row_inside_an_html_comment_is_not_counted,
# added alongside the `_prose()` HTML-comment fix (`_param_rows`/`_names_used` now
# read comment-stripped text too, so a commented-out row or name no longer
# satisfies a positive claim check). `pytest --collect-only -q` reports "1617 tests
# collected", and a full run with MIRAGE_STRICT_SKIPS=1 gives 1604 passed, 13
# skipped, 0 failed -- 1604 + 13 = 1617. 1612 + 5 = 1617.

#
#
# RE-MEASURED 2026-09-03, release 1.0.0 plan 11 Task 9 (pin the figure set's
# registration backend names to RegBackends.methods()). One new case:
# tests/test_figures_match_the_pipeline.py::test_figures_name_exactly_the_shipped_registration_backends.
# `pytest --collect-only -q` reports "1618 tests collected", and a full run
# with MIRAGE_STRICT_SKIPS=1 gives 1605 passed, 13 skipped, 0 failed --
# 1605 + 13 = 1618. 1617 + 1 = 1618.

#
#
# RE-MEASURED 2026-09-03, release 1.0.0 plan 11 Task 11 (phase exit-state
# assertions plus the backend-name guard's possessive-exclusion fold-in). One
# new case: tests/test_figures_match_the_pipeline.py::test_a_shipping_claim_is_not_excused_by_a_possessive_tail,
# a drill proving "mirage ships ashlar's tile solver" is still caught even
# though the possessive exclusion normally lets "ASHLAR's"/"Elastix bindings"
# through. `pytest --collect-only -q` reports "1619 tests collected", and a
# full run with MIRAGE_STRICT_SKIPS=1 gives 1606 passed, 13 skipped, 0 failed
# -- 1606 + 13 = 1619. 1618 + 1 = 1619.
#
# 2026-09-03, plan 07 Tasks 0-6: the real-suite string-vs-path guard, ome_io's dispatch
# table test, the synthesised-format read_info/read_plane suite, the end-to-end
# convert_image format tests and the converter's refusal tests. No deletions, no skips.

#
#
# RE-MEASURED after phase 07 (formats-and-tests). Added: the ome_io dispatch
# table, the convert-image refusals, three bin/ script behaviour files, three
# bin/utils behaviour files, the format-env/convert-image pin guard, and the
# validation-report guard. tests/integration/formats/ is NOT in this count --
# it is excluded from the standard invocation and runs in its own CI job
# (requirements/format-tests.txt).
#
# Measured after generating the fixtures, with the suite's standard exclusions:
# `pytest --collect-only -q` reports "1874 tests collected", and a full run
# with MIRAGE_STRICT_SKIPS=1 gives 1861 passed, 13 skipped, 0 failed --
# 1861 + 13 = 1874, up from the prior entry's 1846 (within FLOOR_DRIFT (50),
# re-ratcheted anyway per instruction rather than left riding inside the band).

#
#
# RE-MEASURED after tests/test_validation_report_is_real.py (Task 14 Step 7):
# 11 new cases (6 plain functions + the 5-way test_every_vendor_format_has_a_row
# parametrisation). `pytest --collect-only -q` reports "1885 tests collected",
# up from the prior entry's 1874. 1874 + 11 = 1885.
#
#
# The value carried here (1797) between the entry above and this one was set by
# other release-1.0.0 plan-12 work in a sibling worktree, not measured in this
# file's own prose -- not re-derived here, just recorded as the starting point.
#
# RE-MEASURED 2026-09-03, release 1.0.0 plan 12 Tasks 9/4/10 (widen the
# CLI-boolean guard to configs and the bare form; params/full_pipeline.json's
# InstanSeg coherence guard; docs/parameters.md<->schema sync guard). 23 new
# cases: tests/test_no_cli_boolean_params_in_docs.py gained 3
# (test_the_scan_covers_the_configuration_files_too,
# test_the_detector_recognises_the_bare_form_inside_a_command,
# test_the_detector_leaves_prose_naming_a_boolean_param_alone);
# tests/test_param_presets_are_coherent.py is new with 16 (2 parametrised over
# 7 presets each, plus 2 plain); tests/test_parameters_doc_matches_schema.py is
# new with 4. `pytest --collect-only -q` reports "1820 tests collected", and a
# full run with MIRAGE_STRICT_SKIPS=1 gives 1807 passed, 13 skipped, 0 failed --
# 1807 + 13 = 1820. 1797 + 23 = 1820.

#
#
#
# RE-MEASURED 2026-09-03, release 1.0.0 plan 12 Task 1 (conf/site.config.template
# becomes the owner of per-site sizing, R4) plus the fix round it triggered. 14
# new cases: tests/test_site_config_template_sizes_a_run.py is new with 3;
# tests/test_param_presets_are_coherent.py gained 8
# (test_a_preset_does_not_bake_in_site_sizing parametrised over 7 presets,
# test_the_site_sizing_exemption_is_not_stale, and
# test_backend_prefixes_covers_exactly_the_schema_backends from the fix round's
# M8); tests/test_parameters_doc_matches_schema.py gained 1
# (test_every_literal_doc_default_matches_nextflow_config, I4);
# tests/test_no_cli_boolean_params_in_docs.py gained 1
# (test_the_detector_recognises_the_equals_form_too, M5).
# test_a_preset_does_not_bake_in_site_sizing[test.json] is a deliberate,
# permanent skip (params/test.json intentionally bakes in tiny self-contained
# ceilings) and is listed in tests/expected_skips.txt so MIRAGE_STRICT_SKIPS
# does not flag it. `pytest --collect-only -q` reports "1834 tests collected",
# and a full run with MIRAGE_STRICT_SKIPS=1 gives 1820 passed, 14 skipped, 0
# failed -- 1820 + 14 = 1834. 1820 + 14 = 1834.

#
#
#
# RE-MEASURED 2026-09-03, release 1.0.0 plan 12 Task 2 (every documented
# `nextflow run` command must actually launch, R4). Five new cases, all in the
# new tests/test_documented_commands_are_runnable.py
# (test_every_documented_command_would_launch,
# test_the_profiles_this_guard_trusts_really_pin_what_it_claims,
# test_the_extractor_joins_continuations_and_skips_prose,
# test_the_detector_recognises_the_command_that_actually_fails,
# test_the_detector_leaves_the_working_forms_alone); no deletions, no other
# test count moved. `pytest --collect-only -q` reports "1839 tests collected",
# and a full run with MIRAGE_STRICT_SKIPS=1 gives 1825 passed, 14 skipped, 0
# failed -- 1825 + 14 = 1839. 1834 + 5 = 1839.
# RE-MEASURED 2026-09-03 on release/p12-docs (plan 12 Task 13, phase exit).
# The earlier plan-12 notes above were written on the pre-rebase branch and
# describe smaller counts; after the rebase onto dev (22c8e15, plans 04-11
# merged) the integrated tree collects 1934: a full run with
# MIRAGE_STRICT_SKIPS=1 gives 1917 passed, 17 skipped, 0 failed --
# 1917 + 17 = 1934. Plan 12 as a whole adds four documentation guards
# (site.config sizing, documented-command runnability, params/*.json
# coherence, parameters.md-vs-schema), the cases widening the CLI-boolean
# guard to configs and to the bare form, and one BLOCKING_CHECKS
# parametrisation for the mkdocs job.
#
# RE-MEASURED 2026-09-04, release 1.0.0 plan 14 Task 2 (collapse the two 1.0.0
# CHANGELOG.md sections into one dated release). Four new cases, all in the new
# tests/test_changelog_is_release_shaped.py (test_no_version_has_two_sections,
# test_the_manifest_version_has_a_dated_section, test_an_unreleased_section_exists,
# test_every_heading_has_a_link_definition); no deletions, no other test count
# moved. Measured after generating the fixtures, with the suite's standard
# exclusions: `pytest --collect-only -q` reports "1938 tests collected", and a
# full run with MIRAGE_STRICT_SKIPS=1 gives 1921 passed, 17 skipped, 0 failed --
# 1921 + 17 = 1938. 1934 + 4 = 1938.
# RE-MEASURED 2026-09-04 on release/p14-release after rebasing onto dev@8a0801e
# (the documented-commands leg became its own CI job, adding one
# BLOCKING_CHECKS parametrisation in tests/test_release_workflow_graph.py):
# 1938 + 1 = 1939.
# RE-MEASURED 2026-09-04, release 1.0.0 plan 03 Task 2 (tests/test_no_todo_author_markers.py,
# four cases: the tree-wide placeholder scan, its scope floor, a detector self-test
# and the TODO(release) exclusion). 1926 passed + 17 skipped = 1943 = 1939 + 4.
# RE-MEASURED 2026-09-04, release 1.0.0 plan 14 Task 3
# (tests/test_citation_metadata_matches_the_manifest.py, three cases). 1943 + 3 = 1946.
# RE-MEASURED 2026-09-06 (cleanup_level gate made lazy: one BLOCKING_CHECKS
# parametrisation in tests/test_release_workflow_graph.py for tests/cleanup_level_pin.sh).
# 1946 + 1 = 1947.
# RE-MEASURED 2026-09-08 (tests/test_valis_preflight.py: eight cases for the VALIS
# pyramid-level -1 preflight and register.py's None-return check) plus the two
# per-module parametrisations that pick up the new bin/utils/valis_preflight.py.
# 1947 + 8 + 2 = 1957.
1957
